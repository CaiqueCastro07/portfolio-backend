import {
    response,
    delay,
    reverseJoinDate
} from "../src/util/helpers"

let count = 0;

describe("HELPERS", () => {

    it(`[${++count}] function 'response()' should return a JSON with { status:[number], message:[string], error:[boolean], data:[any] }`, () => {
    })

    it(`[${++count}] function 'response()' should return a JSON with { status:[number], message:[string], error:[boolean], data:[any] }`, () => {
        console.log("aaaa", "hee")
    })

    it(`[${++count}] function 'response()' should return a JSON with { status:[number], message:[string], error:[boolean], data:[any] }`, () => {
        console.log("conggggawgwtei", "hee")
    })

})